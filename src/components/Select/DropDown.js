import styled from 'react-emotion';

export default styled.div`
  position: absolute;
  right: -220px;
  top: -17px;
  z-index: 10;
  background: white;
  
  box-shadow: 0 10px 18px -2px rgba(0,0,0,0.75);
`;
